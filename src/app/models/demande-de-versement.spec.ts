import { DemandeDeVersement } from './demande-de-versement';

describe('DemandeDeVersement', () => {
  it('should create an instance', () => {
    expect(new DemandeDeVersement()).toBeTruthy();
  });
});
