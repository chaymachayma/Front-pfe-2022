import { DemandeConsultation } from './demande-consultation';

describe('DemandeConsultation', () => {
  it('should create an instance', () => {
    expect(new DemandeConsultation()).toBeTruthy();
  });
});
